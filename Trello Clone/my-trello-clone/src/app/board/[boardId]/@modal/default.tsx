// src/app/board/[boardId]/(@modal)/default.tsx

// File này sẽ được render khi không có route nào trong slot @modal khớp với URL.
// Chúng ta trả về null để không hiển thị gì cả.
export default function Default() {
    return null;
}